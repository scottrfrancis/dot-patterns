---
okf_version: "1.0"
name: dot-patterns
description: Layered design-pattern library (GoF index + house stances, personal patterns, vetted mixins as submodules) for AI coding agents.
---
# dot-patterns — design-pattern library

Served read-only via a dedicated patterns MCP (kb-mcp image) on the LAN, and cloneable
publicly. Entries are `SKILL.md` files under `patterns/` and accepted `mixins/`.
Public source: github.com/scottrfrancis/dot-patterns
