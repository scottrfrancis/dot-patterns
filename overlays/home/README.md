# overlays/home — LAN-only patterns

Homelab / ops-adjacent patterns that are **not for the public mirror**. Served on the LAN
(kb-mcp) and to LAN hosts, never published. `visibility: home` in the frontmatter.

Examples that belong here (not base): patterns that reference your specific homelab
topology, host roles, or the central-ops-knowledge doctrine.
