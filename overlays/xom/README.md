# overlays/xom — XOM-specific patterns

Patterns that travel **only** in the XOM release bundle (the one-way `.ps1` package).
`visibility: xom`. Never published; never in the EY package. Governed by the `xom` scrub
profile on the way into the bundle.
